const Home = () => {
  return (<
    div>
      <h1>Home title</h1>
      <button
        onClick={() => {alert('This home page')}}
      >
        Alert
      </button>
    </div>
  );
};

export default Home;
